<policies>
  <inbound>
    <base />

    <!--
      ── IP filtering (test) ───────────────────────────────────────────────────
      Block known bad actors. Replace placeholders with real CIDRs.
      No IP block on test runner ranges — add exceptions above if needed.
    -->
    <ip-filter action="forbid">
      <address>192.0.2.1</address>
      <address-range from="198.51.100.0" to="198.51.100.255" />
    </ip-filter>

    <!--
      ── Rate limiting (test) ──────────────────────────────────────────────────
      Mirrors prod limits but allows test runners to call freely.
    -->
    <rate-limit-by-key
      calls="40"
      renewal-period="60"
      counter-key="@(context.Subscription.Id)"
      remaining-calls-header-name="X-RateLimit-Remaining"
      retry-after-header-name="Retry-After" />

    <quota-by-key
      calls="1000"
      renewal-period="604800"
      counter-key="@(context.Subscription.Id)" />

    <set-query-parameter name="api-version" exists-action="skip">
      <value>v1</value>
    </set-query-parameter>

    <authentication-managed-identity resource="https://ai.azure.com" />

  </inbound>
  <backend>
    <base />
  </backend>
  <outbound>
    <base />
    <set-header name="X-RateLimit-Policy" exists-action="override">
      <value>test-40rpm-1000pw</value>
    </set-header>
    <!-- Tracing headers -->
    <set-header name="X-Request-Id" exists-action="override">
      <value>@(context.RequestId)</value>
    </set-header>
    <set-header name="X-Environment" exists-action="override">
      <value>test</value>
    </set-header>
    <set-header name="X-Powered-By" exists-action="override">
      <value>L-W Tech AI Gateway</value>
    </set-header>
    <set-header name="Authorization" exists-action="delete" />
  </outbound>
  <on-error>
    <base />
    <choose>
      <when condition="@(context.Response.StatusCode == 429)">
        <return-response>
          <set-status code="429" reason="Too Many Requests" />
          <set-header name="Content-Type" exists-action="override">
            <value>application/json</value>
          </set-header>
          <set-body>@{
            return Newtonsoft.Json.JsonConvert.SerializeObject(new {
              error = new {
                code    = "rate_limit_exceeded",
                message = "Agent rate limit reached (test: 40 rpm / 1 000 pw). Check Retry-After header.",
                details = new { subscription = context.Subscription.Id, policy = "test-40rpm-1000pw" }
              }
            });
          }</set-body>
        </return-response>
      </when>
    </choose>
  </on-error>
</policies>

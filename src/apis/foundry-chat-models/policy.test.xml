<policies>
  <inbound>
    <base />

    <!--
      ── IP filtering (test) ───────────────────────────────────────────────────
      Block known bad actors. Add CIDRs below as needed.
    -->
    <ip-filter action="forbid">
      <!-- RFC 5737 documentation ranges — replace with real IPs to block -->
      <address>192.0.2.1</address>
      <address-range from="198.51.100.0" to="198.51.100.255" />
    </ip-filter>

    <!--
      ── Rate limiting (test) ──────────────────────────────────────────────────
      Moderate cap — mirrors prod behaviour without being as restrictive.
      Allows load/integration tests to run without constant 429s.
    -->
    <rate-limit-by-key
      calls="120"
      renewal-period="60"
      counter-key="@(context.Subscription.Id)"
      remaining-calls-header-name="X-RateLimit-Remaining"
      retry-after-header-name="Retry-After" />

    <!--
      ── Weekly quota ─────────────────────────────────────────────────────────
      Loose ceiling so integration test suites don't exhaust it mid-run.
    -->
    <quota-by-key
      calls="5000"
      renewal-period="604800"
      counter-key="@(context.Subscription.Id)" />

    <authentication-managed-identity resource="https://cognitiveservices.azure.com" />

  </inbound>
  <backend>
    <base />
  </backend>
  <outbound>
    <base />
    <set-header name="X-RateLimit-Policy" exists-action="override">
      <value>test-120rpm-5000pw</value>
    </set-header>
    <!-- Tracing headers -->
    <set-header name="X-Request-Id" exists-action="override">
      <value>@(context.RequestId.ToString())</value>
    </set-header>
    <set-header name="X-Environment" exists-action="override">
      <value>test</value>
    </set-header>
    <set-header name="X-Powered-By" exists-action="override">
      <value>L-W Tech AI Gateway</value>
    </set-header>
    <set-header name="Authorization" exists-action="delete" />
  </outbound>
  <on-error>
    <base />
    <choose>
      <when condition="@(context.Response.StatusCode == 429)">
        <return-response>
          <set-status code="429" reason="Too Many Requests" />
          <set-header name="Content-Type" exists-action="override">
            <value>application/json</value>
          </set-header>
          <set-body>@{
            return Newtonsoft.Json.JsonConvert.SerializeObject(new {
              error = new {
                code    = "rate_limit_exceeded",
                message = "Rate limit reached (test: 120 rpm / 5 000 pw). Check Retry-After header.",
                details = new { subscription = context.Subscription.Id, policy = "test-120rpm-5000pw" }
              }
            });
          }</set-body>
        </return-response>
      </when>
    </choose>
  </on-error>
</policies>
